package Model;

import Model.Store.StoreItem;

public interface ShopService {


/*    public boolean updateStoreItem(int id, StoreItem item){
        if (this.items.remove(getItemById(id))){
            return addStoreItem(item);
        }
        else {
            return false;
        }
    }*/
}
